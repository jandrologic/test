<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página Personal</title>
    <style>
        /* Estilo global */
        body {
            margin: 0;
            overflow: hidden;
            font-family: Arial, sans-serif;
        }

        /* Contenedor de fondo */
        .background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://i.gifer.com/Vy2P.gif') no-repeat center center fixed;
            background-size: cover;
            z-index: -1;
        }

        /* Estilo para la bola */
        .center-ball {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.6), 0 0 50px rgba(255, 255, 255, 0.4);
            animation: glow 2s infinite alternate;
        }

        /* Animación de brillo */
        @keyframes glow {
            from {
                box-shadow: 0 0 20px rgba(255, 255, 255, 0.6), 0 0 50px rgba(255, 255, 255, 0.4);
            }
            to {
                box-shadow: 0 0 30px rgba(255, 255, 255, 0.8), 0 0 60px rgba(255, 255, 255, 0.5);
            }
        }

        /* Estilo del texto */
        .center-ball h1 {
            margin: 0;
            color: #333;
            font-size: 36px;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.9);
            text-align: center;
        }
    </style>
</head>
<body>
    <!-- Fondo animado -->
    <div class="background"></div>

    <!-- Bola con el nombre -->
    <div class="center-ball">
        <h1>SCRIPT</h1>
    </div>
</body>
</html>
